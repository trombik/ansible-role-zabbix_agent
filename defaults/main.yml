---
zabbix_agent_user: "{{ __zabbix_agent_user }}"
zabbix_agent_group: "{{ __zabbix_agent_group }}"
zabbix_agent_service: "{{ __zabbix_agent_service }}"
zabbix_agent_package: "{{ __zabbix_agent_package }}"
zabbix_agent_conf_dir: "{{ __zabbix_agent_conf_dir }}"
zabbix_agent_conf_file: "{{ __zabbix_agent_conf_file }}"
zabbix_agent_flags: ""
zabbix_agent_server: 127.0.0.1
zabbix_agent_listen_port: 10050
zabbix_agent_listen_ip: 0.0.0.0
zabbix_agent_conf_d_dir: "{{ zabbix_agent_conf_dir }}/zabbix_agentd.conf.d"
zabbix_agent_config_flagments: []
zabbix_agent_pid_dir: "{{ __zabbix_agent_pid_dir }}"
zabbix_agent_pid_file: "{{ zabbix_agent_pid_dir }}/zabbix_agentd.pid"
zabbix_agent_log_dir: "{{ __zabbix_agent_log_dir }}"
zabbix_agent_log_file: "{{ zabbix_agent_log_dir }}/zabbix_agentd.log"
zabbix_agent_x509_cert_dir: "{{ zabbix_agent_conf_dir }}/cert"
zabbix_agent_x509_certificates: []
